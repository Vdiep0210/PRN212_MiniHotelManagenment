<Window x:Class="HotelManagement.Views.RoomEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Room Editor" Height="380" Width="400"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Room Number:"/>
        <TextBox x:Name="txtNumber" Grid.Row="0" Margin="0,24,0,8"/>

        <TextBlock Text="Description:" Grid.Row="1" />
        <TextBox x:Name="txtDesc" Grid.Row="1" Margin="0,24,0,8" Height="60" TextWrapping="Wrap"/>

        <TextBlock Text="Price/Day:" Grid.Row="2" />
        <TextBox x:Name="txtPrice" Grid.Row="2" Margin="0,24,0,8"/>

        <TextBlock Text="Capacity:" Grid.Row="3" />
        <TextBox x:Name="txtCapacity" Grid.Row="3" Margin="0,24,0,8"/>

        <TextBlock Text="Room Type:" Grid.Row="4" />
        <ComboBox x:Name="cbRoomType" Grid.Row="4" Margin="0,24,0,8"
                  DisplayMemberPath="RoomTypeName" SelectedValuePath="RoomTypeId" HorizontalAlignment="Left" Width="368"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Save" Width="80" Margin="4" Click="BtnSave_Click"/>
            <Button Content="Cancel" Width="80" Margin="4" Click="BtnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
